with "../../gnat/matreshka_xml.gpr";

project DOM is

   for Object_Dir use ".obj";

   package Compiler is
      for Default_Switches ("Ada") use ("-g", "-gnat12", "-gnatyo");
   end Compiler;

end DOM;

with "../../../gnat/matreshka_league.gpr";

project JSON_Streams is

   for Object_Dir use ".objs";
   for Main use ("demo-driver.adb");

   package Compiler is
      for Default_Switches ("Ada") use ("-g");
   end Compiler;

end JSON_Streams;

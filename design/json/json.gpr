with "../../gnat/matreshka_league.gpr";

project JSON is

   for Object_Dir use ".objs";

   package Compiler is
      for Default_Switches ("Ada") use ("-g", "-gnat12", "-gnatyo");
   end Compiler;

end JSON;

with "../../gnat/matreshka_xml.gpr";

project DOM is

   for Object_Dir use ".objs";
   for Source_Dirs use ("dom");

   package Compiler is
      for Default_Switches ("Ada") use ("-g", "-gnat12", "-gnatymo");
   end Compiler;

end DOM;
